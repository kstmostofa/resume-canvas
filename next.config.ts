import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "avatars.githubusercontent.com"], // Add your image domains here
  },
};

export default nextConfig;
